# 出海投资情报工具 PRD

Version 2.0 —— 简化重构版，替代 v1.0 全部内容

------------------------------------------------------------------------

## 0. 这份文档解决什么问题

v1.0 的 PRD/IA/UX 三份文档是按"彭博终端级别"的复杂度写的（六个一级导航、团队协作、通知中心、告警规则、34 节视觉规范……）。据此实现出来的原型（`investment-intelligence/`）信息繁复、上手成本高，且底层数据源（GDELT 新闻、仅美股 SEC 覆盖）撑不起这个复杂度。

v2.0 的目标：把范围收紧到"输入公司名/代码 → 拿到有来源、可信、能快速判断是否值得关注的信息"这一件事上，做扎实，再谈扩展。

------------------------------------------------------------------------

## 1. 背景与目标用户

目标用户：复星集团投资 CFO 及其出海/跨境投资团队。

真实痛点：跟踪的标的公司横跨美股中概股、A股、港股，每天要人工搜索新闻、公告、财报、监管动态、行业信息，耗时且没有统一的信息组织方式，也难以快速追溯"这条信息到底来自哪里、可不可信"。

这个工具不是投资建议系统，是**信息收集与初筛工具**。最终的投资判断仍由人做出；工具的职责是把分散信息聚合、打上来源标签、按确定性规则做重要度初筛，把分析师从"到处搜索"里解放出来。

------------------------------------------------------------------------

## 2. 部署

- 唯一正式路径：`https://olifeng.com/tool`（替代 v1.0 中的 `/investment-intelligence/` 方案）
- 访问控制：**简单密码保护**（一个共享口令 + 环境变量校验即可，不需要账号体系/登录页），防止陌生人访问消耗第三方接口额度
- 作为独立模块运行，不依赖主作品集页面的样式/脚本，可以独立部署、独立迭代

------------------------------------------------------------------------

## 3. 产品边界：做什么 / 明确不做什么

### 做（V1 范围）

- 输入公司名或股票代码，聚合展示该公司近期的新闻、公告(filings)、监管动态、行业相关信息
- 每条信息都标注来源，来源可一键查看完整信息（发布方、时间、原文链接）
- 用**确定性规则**（非 AI 推理）标注信息的重要度，并给出"为什么可能值得关注"的简短理由
- 首页维护一个"关键更新日志"，聚合用户已关注公司的最新动态

### 明确不做（防止范围再次膨胀）

- 不做多用户账号体系、团队协作、评论/@提及
- 不做通知中心、邮件/Slack/企业微信推送（列入路线图，非 V1）
- 不做真正的 AI 语义生成或投资建议（V1 用规则模板；界面上必须清楚标注这是规则筛选，不能让用户误以为是 AI 分析）
- 不做服务器端持久化或跨设备同步（V1 关注列表存浏览器本地）
- 不做估值模型、选股、组合分析

------------------------------------------------------------------------

## 4. 覆盖范围与数据源架构（核心决策，直接决定技术方案）

"出海/跨境"属性决定了这不能是单一市场工具，但三个市场的数据可得性差异很大，必须分层设计并在 UI 上诚实标注每层的完整度，绝不能让某个市场数据稀疏时看起来像"没有新闻"。

### Tier 1 —— 美股（含中概股 ADR），高可靠性，V1 必须做扎实

- 公司信息 / 公告(filings) / 财务数据：SEC EDGAR 公开接口（`data.sec.gov`），免费、稳定、官方权威，现有原型已验证可用
- 覆盖阿里巴巴、拼多多、京东、蔚来等在美股上市的中概股 ADR —— 这对出海场景特别重要，很多复星关注的跨境标的或对标公司本身就是美股上市主体
- 行情快照：现有的 Nasdaq 公开接口可以保留，但必须做 best-effort 容错（非官方接口，随时可能失效），失效时明确显示"行情数据当前不可用"而不是报错崩溃

### Tier 2 —— A股，中等可靠性

- 公告数据：巨潮资讯网 (cninfo.com.cn) 的公开信息披露查询接口，无需 API key，是国内被广泛验证可用的免费公开数据源
- 覆盖沪深上市公司的重大事项公告、定期报告、监管问询函等
- 财务数据：V1 不强求结构化财务数字，能拿到公告级别信息即可，结构化财报数据留给 V2
- 股票代码格式需支持 6 位数字代码（如 600519）以及带交易所后缀的写法（600519.SH / 000001.SZ）

### Tier 3 —— 港股，尽力而为（best-effort）

- 公告数据：港交所披露易 (HKEXnews) 公开页面，没有官方 JSON API，需要谨慎抓取并遵守合理的请求频率
- V1 允许这一层信息不完整，但 UI 必须明确提示"港股数据覆盖有限"，不能沉默地返回空列表让用户误以为公司没有动态
- 代码格式支持 5 位港股代码（如 00700.HK）

### 新闻源（三层市场通用）

- 用 **Google News RSS**（免费、无需 key、原生支持中英文查询、可按公司名/代码检索）替代现有原型里的 GDELT —— GDELT 噪音大、相关性差，不适合作为专业工具的主力新闻源
- 每条新闻标注来源域名，并按一个简单的权威度白名单打分（如路透、彭博、财新、证券时报、界面新闻等 = 高权威度；其余 = 一般权威度）；白名单可持续维护，不追求一次做全

### 数据源诚实性原则（贯穿所有层级）

任何接口失败或数据缺失时，界面必须清楚显示"该来源当前不可用"或"该市场数据覆盖有限"，绝不能编造内容、也不能用空白列表制造"没有动态"的错误印象。这是比功能丰富度更高优先级的要求。

------------------------------------------------------------------------

## 5. 核心功能（MVP，唯一必须完成的范围）

### 5.1 公司搜索与工作台 —— 最高优先级

全局搜索框，支持：

- 公司名（中文/英文均可）
- 股票代码：美股 ticker、A股 6 位代码（含交易所后缀）、港股 5 位代码
- 自动判断输入属于哪个市场，路由到对应的数据源

命中后进入"公司工作台"页面，包含：

**公司概览**
名称、代码、交易所、行业、（若数据可得）市值/股价快照。

**信息流**
按时间倒序聚合四类事件——新闻 / 公告(filings) / 监管动态 / 行业相关。每条卡片包含：

- 类型标签、时间戳
- 标题、一句话摘要（用原文标题或首句，V1 不做 AI 改写，避免生成不准确的转述）
- 重要度标签：高 / 中 / 低（见 5.3 规则打分）
- 来源 chip，点击展开来源详情面板

**初筛区块："是否可能影响投资判断"**
用规则模板生成 2-4 条要点，例如：

- 触发关键词"重大资产重组 / 关联交易 / 立案调查" → 标记为高优先级并说明触发原因
- 检测到新的定期报告（10-K/10-Q/年报） → 提示"建议复核财务指引变化"

界面上必须用明显的标签注明"规则筛选结果，非 AI 生成的投资分析"，避免用户误判工具的能力边界。

### 5.2 首页——关键更新日志

- 用户在公司工作台点击"加入关注"，写入浏览器 localStorage（不跨设备/浏览器同步，这是 V1 的已知限制，写入路线图）
- 首页加载时，对关注列表里的每家公司发起实时抓取，把结果按时间倒序合并渲染成一条"日志流"
- 空状态（还没有关注任何公司）：引导用户去搜索并添加第一家公司，而不是展示空白页面
- 页面上必须说明"这是每次打开页面时的实时快照，不是后台持续监控"，避免用户误以为工具在离线时也在追踪

### 5.3 重要度打分规则（规则引擎，非 AI）

基于确定性规则，例如：

- 事件类型：监管性公告(重组/立案/问询函/退市风险) > 财报类(10-K/10-Q/年报/季报) > 常规公告 > 一般新闻
- 关键词命中：维护一份中英文关键词库（如"重大资产重组""立案调查""业绩预警""material acquisition""investigation"等），命中即提升重要度
- 时效性：72 小时内的信息优先展示

这套规则应该做成可配置的列表（而不是硬编码判断树），方便后续迭代关键词库而不用改代码逻辑。

### 5.4 来源可追溯层（硬性要求，贯穿全局）

- 任何结论性文字（重要度标签、初筛要点）旁边必须有可点击的来源标记
- 点击展开来源面板，包含：完整标题、发布方、发布时间、原文直达链接（新标签页打开——大部分财经网站/公告系统禁止 iframe 嵌入，不要在这上面浪费工程时间）
- 来源按权威度分级展示：监管公告/交易所披露 > 主流财经媒体 > 一般媒体
- 无法验证的信息必须显式提示"无法独立核实"，不得省略或留空

------------------------------------------------------------------------

## 6. 视觉与体验原则

- 简洁干净，不追求信息密度最大化——这是对 v1.0 UX 规范（彭博终端风格）的明确修正
- 中文优先界面（目标用户是中文母语的投资团队），但要自然支持中英混排（公司名、ticker、来源标题经常是英文）
- 两屏结构：首页 + 公司工作台，不引入额外的导航层级
- 不需要培训即可上手，是比"功能全"更高的优先级

------------------------------------------------------------------------

## 7. V1 技术约束（明确写给 Codex）

- 无 LLM 调用；所有"重要度/初筛"结论来自 5.3 的确定性规则引擎
- 无服务器端数据库；关注列表存浏览器 localStorage
- 简单密码保护整个工具：环境变量校验一个共享口令，不需要完整登录系统
- 页面结构：`/tool`（首页）+ `/tool/company/:id`（公司工作台），不要更多顶级导航
- 现有的 `api/investment-intelligence.js`（SEC EDGAR 部分）可以复用和扩展，不需要推倒重做；需要新增 A股(cninfo)、港股(HKEXnews)、Google News RSS 三个数据源模块

------------------------------------------------------------------------

## 8. 成功标准

1. 用户能在 2 分钟内看懂一家公司近期发生了什么
2. 任意结论一键可查证到原始来源，不存在"无来源的断言"
3. 三个市场中，美股体验必须完整可靠；A股/港股体验可以不完整，但必须诚实、不误导
4. 界面干净，不需要培训即可上手（这是对 v1.0 过度设计的直接纠正）

------------------------------------------------------------------------

## 9. 路线图（V2+，明确不在本次实现范围内）

- 接入真实 LLM（如 Claude API）做语义摘要、归纳与更细致的投资影响判断
- 服务器端持久化 + 定时抓取，实现真正的"持续监控"而非"打开即抓取"
- A股/港股数据源升级为更稳定的数据商（可能需要付费）
- 多用户/团队协作、评论、@提及
- 邮件 / 企业微信日报推送
- 结构化财报数据解析（A股/港股）

------------------------------------------------------------------------

## 10. 给 Codex 的实现优先级

1. 来源可靠性与可追溯性（不可妥协，是整个工具的信任基础）
2. 数据源分层接入：Tier 1(美股) 必须稳定可靠，Tier 2/3(A股/港股) 尽力而为并诚实降级
3. 搜索与公司工作台主流程
4. 首页日志聚合视图
5. 视觉打磨——保持极简，不要照搬 v1.0 UX 规范里的高密度终端风格
