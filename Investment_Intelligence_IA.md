# 出海投资情报工具 —— 信息架构 (IA)

Version 2.0 —— 替代 v1.0 全部内容

## 目的

v1.0 的 IA 是按多用户企业级平台设计的（六个一级导航、Watchlist/Alerts/Notification 各自独立页面、面包屑、快捷键体系）。v2.0 把结构收紧到 [PRD v2.0](Investment_Intelligence_PRD.md) 定义的 MVP 范围：一个人用、无账号体系、无服务器持久化的两页工具。

------------------------------------------------------------------------

## 页面结构

只有两个核心页面，外加一个可以嵌入首页而不必独立成页的设置入口：

```
/tool                     首页：搜索入口 + 关注列表日志流
/tool/company/:market/:id 公司工作台：概览 + 信息流 + 来源
```

不设置独立的 Watchlist / Alerts / Notification Center / Search Results 页面——这些在 v1.0 里是为多用户团队协作场景设计的，与当前"个人使用、localStorage 存储"的技术约束不匹配。关注列表管理直接嵌入首页顶部。

访问密码校验发生在应用外层（中间件/网关级别），不作为一个可导航的"页面"存在。

------------------------------------------------------------------------

## 导航模型

不需要持久侧边栏。顶部只有一个常驻元素：**全局搜索框**，输入公司名或代码后跳转到公司工作台。这是整个工具唯一的导航入口,加上"返回首页"的 logo/标题点击。

原因:两页结构下,复杂导航系统本身就是多余的复杂度。

------------------------------------------------------------------------

## 首页 (`/tool`)

```
首页
├── 全局搜索框（顶部,常驻）
├── 关注列表管理条
│   └── 已关注公司 chips(可点击跳转,可移除)
├── 日志流(核心内容区)
│   └── 合并展示所有关注公司的最新事件,按时间倒序
└── 空状态(无关注公司时)
    └── 引导搜索并添加第一家公司
```

日志流里的每一项本质上和公司工作台信息流里的卡片是同一种组件(见下文"事件卡片"),只是跨公司合并展示,并额外标注来自哪家公司。

------------------------------------------------------------------------

## 公司工作台 (`/tool/company/:market/:id`)

```
公司工作台
├── 头部
│   ├── 公司名 / 代码 / 交易所 / 行业
│   ├── (若数据可得)市值 / 股价快照
│   └── "加入关注" / "取消关注" 按钮
├── 初筛区块
│   └── 规则引擎生成的 2-4 条"是否值得关注"要点,附来源
├── 信息流(核心内容区)
│   ├── 类型筛选:全部 / 新闻 / 公告 / 监管动态 / 行业
│   └── 事件卡片列表,按时间倒序
└── 数据覆盖提示
    └── 若当前市场(A股/港股)数据源降级,在此处明确提示
```

不设置独立的 Overview / News / Filings / Earnings / Timeline / Sources / Notes 七个 tab(v1.0 的设计)。一个信息流 + 一个类型筛选器就能覆盖 MVP 需求,tab 越多,用户理解成本越高。

------------------------------------------------------------------------

## 核心组件:事件卡片

首页日志流和公司工作台信息流共用同一个组件,是整个 IA 里唯一需要精心设计的复用单元:

```
事件卡片
├── 类型标签(新闻 / 公告 / 监管动态 / 行业)
├── 时间戳
├── (仅首页日志流)所属公司标签
├── 标题
├── 一句话摘要(原文标题或首句,不做 AI 改写)
├── 重要度标签(高 / 中 / 低,规则引擎生成)
└── 来源 chip → 点击展开来源详情面板
    ├── 完整标题
    ├── 发布方
    ├── 发布时间
    └── 原文直达链接(新标签页打开)
```

------------------------------------------------------------------------

## 信息层级

```
原始来源
    ↓
事件卡片(带来源 chip)
    ↓
(可选)初筛要点 —— 引用具体事件卡片作为依据
```

v1.0 里的"Narrative → Event → Evidence → Original Source"四层模型被简化为三层,并且明确去掉了需要真正 NLP 能力才能撑起的"AI Daily Brief 市场叙事"层级——V1 没有接入 LLM,不应该在结构上暗示有这种能力。

------------------------------------------------------------------------

## 明确从 v1.0 移除的结构

- 独立的 Watchlist / Alerts / Notification Center 页面(个人工具、无推送,不需要)
- 六项一级导航 + 用户菜单(两页工具不需要)
- Source Explorer 独立探索页(来源查看直接内嵌在事件卡片里就够用)
- Market Themes 主题聚类模块(需要 NLP 聚类能力,V1 没有)
- Related Entities 关系图谱(V2+ 路线图,不在 MVP)
- 面包屑导航(只有两级页面,不需要)
