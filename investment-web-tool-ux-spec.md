# 出海投资情报工具 —— UX 规范

Version 2.0 —— 替代 v1.0 全部内容（原文件从 34 节的企业级终端规范收紧为两页个人工具规范）

## 0. 产品定义

参见 [PRD v2.0](Investment_Intelligence_PRD.md) 与 [IA v2.0](Investment_Intelligence_IA.md)。这份文档只回答"How"——两个页面(首页、公司工作台)具体长什么样、组件怎么交互。

### 0.1 设计目标

简洁干净,不需要培训即可上手。信息可以密集,但组织必须清晰。v1.0 规范里"彭博终端级别的信息密度"不是本次目标——本工具服务单个 CFO/分析师的日常查阅,不是多屏交易终端。

### 0.2 语言

中文优先界面。导航、按钮、标签、空状态、错误提示默认中文。公司名、股票代码、来源标题保留原文(常为英文),做好中英混排的字重与间距处理,不追求生硬的"整页翻译腔"。

------------------------------------------------------------------------

## 1. 页面清单

只有两个页面,严格对应 IA:

- 首页 `/tool`
- 公司工作台 `/tool/company/:market/:id`

不要在实现过程中引入额外的独立页面(如单独的搜索结果页、设置页)。功能需要嵌入就嵌入这两页里。

------------------------------------------------------------------------

## 2. 全局布局

### 2.1 结构

单栏居中布局,不使用 v1.0 规范里的"左侧导航栏 + 主画布 + 右侧上下文面板"三栏壳层——两页工具不需要持久侧边栏。

- 顶部:一条常驻的头部栏,包含 logo/标题(点击返回首页)+ 全局搜索框
- 主体:单栏内容区,最大宽度 960px,居中,两侧留白
- 来源详情用**右侧滑出面板**(不是独立三栏常驻区),默认收起,点击来源 chip 时滑入

### 2.2 网格与间距

- 使用 8px 间距系统:4 / 8 / 12 / 16 / 24 / 32 / 48
- 内容区左右 padding:桌面 32px,移动端 16px
- 卡片之间垂直间距:16px;分区之间:32px

------------------------------------------------------------------------

## 3. 头部与搜索

### 3.1 头部栏

- 左侧:工具名称(点击返回首页)
- 右侧:全局搜索框,任何页面都可以直接输入公司名/代码跳转

### 3.2 搜索交互

- 输入框 placeholder 用真实示例演示三个市场:"如 拼多多 / PDD / 600519 / 00700.HK"
- 自动判断输入是公司名还是代码,以及属于哪个市场(美股/A股/港股),不需要用户手动选择市场
- 搜索无结果时,明确提示"未找到匹配公司",并给出"检查代码格式"的引导,而不是空白页面

------------------------------------------------------------------------

## 4. 首页

### 4.1 布局顺序

1. 头部(含搜索)
2. 关注列表管理条——已关注公司以 chip 形式横向排列,可点击跳转、可点 x 移除
3. 日志流标题 + "最后更新于 HH:MM" 时间戳 + 手动刷新按钮
4. 日志流:事件卡片纵向列表(见第 6 节)

### 4.2 空状态

无关注公司时,不展示空白日志流,而是展示一个引导块:"还没有关注任何公司,搜索一家公司开始"+ 搜索框焦点提示。

### 4.3 数据刷新说明

日志流上方需要一行小字说明:"此列表为你打开页面时的实时抓取结果,不代表后台持续监控"。这是诚实披露 V1 技术限制的强制要求,不是可选的措辞。

------------------------------------------------------------------------

## 5. 公司工作台

### 5.1 头部区块

- 公司名(大字号)+ 代码 + 交易所徽标/文字 + 行业标签
- 若行情数据可得:显示最新价、涨跌幅(用颜色区分涨跌,红涨绿跌遵循中国市场惯例);若不可得,显示"行情数据当前不可用",不留空白也不报错
- 右上角:"加入关注 / 已关注"按钮,一键切换

### 5.2 初筛区块

- 标题明确写"规则筛选结果",旁边加一个小图标+ tooltip 解释"基于关键词与事件类型的规则判断,非 AI 生成的投资分析"——这是防止用户误判工具能力边界的关键交互,不能省略
- 每条要点后面跟一个来源 chip,点击可以直接定位到对应的事件卡片或打开来源面板
- 若当前公司没有触发任何规则要点,显示"近期没有触发重要度规则的事件",而不是隐藏整个区块

### 5.3 信息流

- 顶部一排筛选 chip:全部 / 新闻 / 公告 / 监管动态 / 行业,单选,点击切换
- 下方是事件卡片纵向列表(见第 6 节)
- 若某个数据源(A股 cninfo / 港股 HKEXnews)当前不可用或数据稀疏,在信息流顶部用一条不刺眼的提示条说明,例如:"港股公告数据覆盖有限,建议同时查阅官方披露易网站"

------------------------------------------------------------------------

## 6. 事件卡片(核心复用组件)

### 6.1 结构

自上而下:

1. 第一行:类型标签(小徽标)+ 时间戳(靠右对齐)+(仅首页日志流场景)所属公司名
2. 标题(可点击展开摘要/来源,或直接是本条卡片的主文字)
3. 一句话摘要——用原文标题或首句,不做 AI 改写,避免生成不准确的转述
4. 底部一行:重要度标签(高/中/低)+ 来源 chip(如 `[路透社]` `[SEC 公告]` `[巨潮资讯]`)

### 6.2 重要度标签样式

- 高:红色/警示色徽标
- 中:琥珀色徽标
- 低:灰色徽标,视觉上弱化,不与高/中争夺注意力

不使用数字置信度百分比这类假精确度的表达方式。

### 6.3 来源 chip 与来源面板

- 来源 chip 是卡片上唯一的强交互元素,点击后从右侧滑入来源面板(见第 7 节),不跳转页面、不打断当前阅读位置
- 面板收起前,滚动位置保持不变

------------------------------------------------------------------------

## 7. 来源详情面板

### 7.1 触发方式

点击任意事件卡片的来源 chip,或初筛要点旁的来源 chip。

### 7.2 面板内容

- 完整标题
- 发布方(媒体名称 / 监管机构 / 交易所)
- 发布时间
- 权威度标签:监管公告/交易所披露 = "官方来源";主流财经媒体(路透/彭博/财新等白名单)= "权威媒体";其余 = "一般来源"
- "查看原文"按钮,新标签页打开原始链接(不做 iframe 内嵌——大部分财经站点和公告系统禁止嵌入,不要为此浪费工程时间)

### 7.3 面板尺寸与动效

- 宽度 400px,桌面端从右侧滑入,移动端全屏覆盖
- 动效 150ms ease-out,足够表达"这是同一上下文的延伸",不需要更复杂的动画系统

### 7.4 无法验证的信息

若某条陈述缺少可点击来源,不要允许它以陈述句形式单独存在——要么补上来源 chip,要么改写为"无法独立核实此信息"并弱化视觉权重。

------------------------------------------------------------------------

## 8. 状态设计

### 8.1 加载状态

- 搜索中:输入框旁的小 spinner,不用全屏 loading
- 信息流加载中:骨架屏卡片(2-3 个),不用旋转图标霸占整个内容区
- 数据源部分失败、部分成功:已成功的部分正常展示,失败的部分展示对应的降级提示条(见 5.3),不要因为一个数据源失败就让整页报错

### 8.2 空状态

- 首页无关注公司(见 4.2)
- 公司信息流筛选后无结果:"该类型下暂无近期动态"+ 一键"查看全部类型"

### 8.3 错误状态

- 搜索不到公司:提示 + 格式引导(见 3.2)
- 访问密码错误:提示"口令不正确",不透露具体校验逻辑
- 接口整体超时:提示"部分数据加载失败,可重试",保留已加载成功的内容,不整体清空重来

------------------------------------------------------------------------

## 9. 排版

- 中文字体栈:`PingFang SC`, `Noto Sans SC`, `Microsoft YaHei`, sans-serif;英文/数字沿用同一字体栈,财务数字使用等宽数字(tabular numerals)
- 字号层级(仅四级,够用即可):标题 24/32、卡片标题 16/24、正文 14/22、元信息(时间/来源)12/18
- 中文场景下不使用全大写变换、不做大幅字母间距拉伸——这些是拉丁文排版的层级手段,不适用于中文
- 中英混排时,数字/代码/百分比与中文字符之间保留自然空隙,不要挤在一起

------------------------------------------------------------------------

## 10. 颜色

- 中性色为主:背景白/浅灰,文字深灰/黑
- 语义色仅用于:重要度标签(红/琥珀/灰)、涨跌幅(红涨绿跌)、来源权威度标签
- 不用颜色作为唯一的信息载体——重要度、权威度都同时配文字标签,不能仅靠颜色区分(可访问性要求)

------------------------------------------------------------------------

## 11. 响应式

- 移动端(<640px):单栏,来源面板变为全屏覆盖而非侧滑
- 桌面端:内容区最大宽度 960px 居中,不需要为宽屏做多栏铺满设计——密集的多栏布局是 v1.0 里被明确废弃的方向

------------------------------------------------------------------------

## 12. 从 v1.0 移除的规范章节(明确不实现)

以下 v1.0 章节对应的功能不在 IA v2.0 范围内,故其 UX 规范一并移除:搜索结果三栏页(无独立搜索页)、AI Answer Mode(无 LLM)、Source Explorer 独立页、公司七 tab 结构、Watchlist 团队类型与多视图、Monitoring Dashboard 预设、Notification Center、键盘快捷键体系、无障碍深度规范(WCAG 细则留到有真实用户反馈后再补)、动效系统(仅保留第 7.3 节这一处滑出动效即可)。

如果后续迭代把范围扩展回团队协作/持续监控/LLM 摘要,应回到 [PRD v2.0](Investment_Intelligence_PRD.md) 的路线图章节评估,再决定是否需要恢复对应的 UX 规范,而不是直接从 v1.0 旧文件里复制回来。
